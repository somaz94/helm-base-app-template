{{- if .Values.ingressApiDocs.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "base-azure.fullname" . }}-api-docs
  labels:
    {{- include "base-azure.labels" . | nindent 4 }}
  {{- with .Values.ingressApiDocs.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- with .Values.ingressApiDocs.className }}
  ingressClassName: {{ . }}
  {{- end }}
  {{- if .Values.ingressApiDocs.tls }}
  tls:
    {{- range .Values.ingressApiDocs.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.ingressApiDocs.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- with .pathType }}
            pathType: {{ . }}
            {{- end }}
            backend:
              service:
                name: {{ include "base-azure.fullname" $ }}
                port:
                  number: {{ $.Values.service.port }}
          {{- end }}
    {{- end }}
{{- end }}
